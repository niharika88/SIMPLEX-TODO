$('<%= j render "form" %>').on('hidden.bs.modal', function (e) {
  $(this).remove();
  $('.modal-backdrop').remove();
}).modal();

$('#label-modal').submit(function() {
    $('#label-modal').modal('hide');
})